export enum ETypeButton {
  TEXT = "text",
  LINK = "link",
  DEFAULT = "default"
}

export enum EShapeButton {
  DEFAULT = "default",
  PLAIN = "plain",
  ROUND = "round",
  CIRCLE = "circle",
  SECONDARY = "secondary"
}
